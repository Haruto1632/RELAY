defaults: [../base.yaml, ../presets/pilot_core.yaml]
environment:
  communication:
    mode: when_who
    cost_per_recipient_attempt: 0.01
    packet_loss_probability: 0.1
experiment_name: o1-loss
locked_fields:
  - environment.environment_version
  - environment.preset
  - environment.grid
  - environment.team
  - environment.incidents
  - environment.sensors
  - environment.motion
  - environment.horizon
  - environment.communication.mode
