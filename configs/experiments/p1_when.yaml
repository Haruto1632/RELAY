defaults: [../base.yaml, ../presets/pilot_core.yaml]
environment:
  communication: {mode: when_broadcast, cost_per_recipient_attempt: 0.01}
experiment_name: p1-when
locked_fields:
  - environment.environment_version
  - environment.preset
  - environment.grid
  - environment.team
  - environment.incidents
  - environment.sensors
  - environment.motion
  - environment.horizon
  - environment.communication.mode
